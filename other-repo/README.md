# other-repo

This is a simple secondary repository used to demonstrate cross-repo checkout in GitHub Actions.

It contains a `scripts/hello.sh` script that prints a greeting and an optional argument.

Usage:

```bash
bash scripts/hello.sh world
```