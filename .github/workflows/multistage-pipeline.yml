name: Simple Multistage Pipeline

on:
  push:
    branches:
      - main

jobs:
  # BUILD STAGE - RUNS FIRST
  build:
    name: Build Application
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          echo "🔨 Building application..."
          echo "Timestamp: $(date)"
          echo "Files compiled successfully"

  # DEVELOPMENT STAGE - DEPENDS ON BUILD
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Development
        run: |
          echo "✅ Deploying to Development..."
          echo "Build Status: SUCCESS ✓"
          echo "Timestamp: $(date)"

  # STAGING STAGE - DEPENDS ON DEV
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: deploy-dev
    steps:
      - uses: actions/checkout@v4
      - name: Run Tests
        run: |
          echo "🧪 Running tests on staging..."
          echo "Dev Status: SUCCESS ✓"
          echo "Checking file integrity..."
      - name: Deploy to Staging
        run: |
          echo "✅ Deploying to Staging..."
          echo "Timestamp: $(date)"

  # PRODUCTION STAGE - DEPENDS ON STAGING
  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-staging
    environment:
      name: production
    steps:
      - uses: actions/checkout@v4
      - name: Pre-deploy Checks
        run: |
          echo "🔍 Running pre-deployment checks..."
          echo "Staging Status: SUCCESS ✓"
          echo "Verifying all files..."
      - name: Deploy to Production
        run: |
          echo "✅ Deploying to Production..."
          echo "Timestamp: $(date)"
          echo "⚠️ LIVE PRODUCTION DEPLOYMENT - All tests passed!"
